#include "main.h"

/**
 * main - This calls the function that prints both positive and negative values.
 * 
 * Return: 0.
*/

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
